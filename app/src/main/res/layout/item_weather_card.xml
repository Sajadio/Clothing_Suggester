<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/rounded_background_card_weather"
    android:gravity="center"
    android:layout_marginStart="16dp"
    android:orientation="vertical">

    <TextView
        android:id="@+id/text_view_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:fontFamily="@font/poppins"
        android:gravity="center"
        android:textColor="@color/gray"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="@+id/image_view_weather_icon"
        app:layout_constraintStart_toStartOf="@+id/image_view_weather_icon"
        app:layout_constraintTop_toBottomOf="@+id/image_view_weather_icon"
        tools:text="00:00" />

    <ImageView
        android:id="@+id/image_view_weather_icon"
        android:layout_width="64dp"
        android:layout_height="64dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_launcher_background" />

    <TextView
        android:id="@+id/text_view_temp_weather"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:fontFamily="@font/poppins"
        android:gravity="center"
        android:textColor="@color/gray"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="@+id/text_view_description_weather"
        app:layout_constraintStart_toStartOf="@+id/text_view_description_weather"
        app:layout_constraintTop_toBottomOf="@+id/text_view_description_weather"
        tools:text="0°" />

</LinearLayout>

